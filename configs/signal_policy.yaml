status: stage_08_configured_v2_stage06_policy_lock

input:
  prediction_file: results/predictions/06_walk_forward_validation_predictions.csv
  selected_model: xgboost
  selected_feature_set: I_full_40
  expected_oof_rows: 51840
  expected_fold_counts:
    1: 10344
    2: 10408
    3: 10397
    4: 10319
    5: 10372

policy:
  implementation_schema_version: stage06_v4_daily_top_fraction_false_positive_priority
  policy_type: daily_cross_sectional_top_fraction
  selected_fraction: 0.05
  minimum_signals_per_date: 1
  quota_rounding: ceil
  ranking_order:
    - score_descending
    - symbol_ascending
    - event_id_ascending
  expected_total_oof_signals: 3016
  expected_fold_signal_counts:
    1: 561
    2: 566
    3: 594
    4: 624
    5: 671

score_policy:
  selected_calibration_method: raw_identity
  probability_calibrator_fitted: false
  probability_threshold_selected: false
  literal_probability_interpretation_allowed: false
  cross_sectional_ranking_allowed: true

safeguards:
  policy_reselected_in_stage08: false
  calibration_candidates_evaluated: false
  signal_fraction_candidates_evaluated: false
  hard_started_filter_applied: false
  hard_breadth_filter_applied: false
  economic_returns_used_for_selection: false
  portfolio_metrics_used_for_selection: false
  unseen_test_loaded: false
  unseen_test_labels_used: false
  model_retrained: false

rules:
  - Consume the Stage 06 selected model, feature set, trial, and fixed daily policy without modification.
  - Filter OOF predictions by both model_name and feature_set_name.
  - Do not search candidate calibration methods.
  - Do not search candidate signal fractions.
  - Use raw XGBoost scores only for same-day ranking.
  - Do not select a fixed probability threshold.
  - Do not open unseen-test files or labels.
