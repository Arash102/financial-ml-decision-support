# Reproducibility Manifest Requirements

Every run must record:
- Git commit SHA
- Git tag when a stage is frozen
- input-file inventory and optional hashes
- configuration snapshot and hash
- random seeds
- Python and package versions
- operating system
- training and inference time
- model hyperparameters
- probability threshold
- output locations
- failed or excluded symbols
